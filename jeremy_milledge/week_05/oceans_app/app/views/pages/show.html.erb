<h2><%= @ocean.name %></h2>
<%= image_tag @ocean.image, :alt=> @ocean.name if @ocean.image.present? %>

<p>Area: <%= number_to_human @ocean.area %> km<sup>2</sup></p>
<p>Maximum Depth: <%= @ocean.depth %>m</p>
<p>Maximum Salinity: <%= number_to_human @ocean.max_salinity %> parts per thousand</p>

<div class="controls">
  <%= link_to 'Edit Ocean', edit_ocean_path(@ocean.id) %>
  <%= button_to 'Delete Ocean', ocean_path(@ocean.id), :method=> 'delete' %>
</div>
